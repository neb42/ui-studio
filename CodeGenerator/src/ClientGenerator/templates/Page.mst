{{=<% %>=}}
import React from 'react';
import ReactDOM from 'react-dom';
import styled from 'styled-components';

<% >recursive_import %>

const <% type %>_<% name %> = () => {
  return React.createElement('div', {}, <% >recursive_element %>);
};

export default Page;