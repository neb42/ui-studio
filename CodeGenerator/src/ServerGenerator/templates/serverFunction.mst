{{ #serverFunctions }}
import {{ . }} from './{{ . }}';
{{ /serverFunctions }}
{{ #queries }}
import {{ . }} from '../queries/{{ . }}';
{{ /queries }}

const {{ name }} = ({
  {{ #clientDependencies }}
  {{ . }},
  {{ /clientDependencies }} 
}) => {
  {{{ functionString }}}
};

export default {{ name }};