{
  "name": "canvas-builder",
  "version": "0.1.2",
  "description": "> TODO: description",
  "author": "Ben McAlindin <bmcalindin@faculty.ai>",
  "homepage": "https://github.com/neb42/canvas/tree/master/packages/builder#readme",
  "license": "MIT",
  "main": "build/server.js",
  "bin": {
    "canvas-builder": "build/server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/neb42/canvas.git"
  },
  "scripts": {
    "clean": "rm -rf build/*",
    "build:server": "yarn clean && tsc",
    "build:client": "cd client && yarn build && cp -r build/ ../build/client",
    "build": "yarn build:server && yarn build:client",
    "lint": "eslint '*/**/*.{js,ts,tsx}' --quiet --fix",
    "dev": "yarn build:server && yarn start --path /Users/bmcalindin/workspace/ExampleApp",
    "start": "node ."
  },
  "dependencies": {
    "@types/yargs": "^15.0.5",
    "canvas-render-engine": "^0.1.2",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "get-port": "^5.1.1",
    "nodemon": "^2.0.4",
    "open": "^7.3.1",
    "socket.io": "^2.3.0",
    "yargs": "^16.0.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.7",
    "@types/express": "^4.17.7",
    "@types/socket.io": "^2.1.11"
  }
}
