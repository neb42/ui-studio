import { defaultTokens } from '@faculty/adler-tokens';

export const adlerTheme = {
  colors: defaultTokens.colors,
  fonts: defaultTokens.fonts,
  header: defaultTokens.header,
  boxshadow: defaultTokens.boxshadow,
};
