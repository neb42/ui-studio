import * as React from 'react';

{{ #noEntryPoints }}
const App = ({ children }: any) => children;
{{ /noEntryPoints }}
{{ ^noEntryPoints }}
{{ #entryPoints }}
import {{ . }} from './{{ . }}';
{{ /entryPoints }}

const App = ({ children }: any) => (
  {{ #entryPoints }}
  <{{ . }}>
  {{ /entryPoints }}
  {children}
  {{ #reverseEntryPoints }}
  </{{ . }}>
  {{ /reverseEntryPoints }}
);
{{ /noEntryPoints }}

export default App;
