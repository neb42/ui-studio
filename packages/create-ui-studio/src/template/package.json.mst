{
  "name": "{{{ name }}}",
  "private": true,
  "version": "0.1.0",
  "description": "> TODO: description",
  "license": "MIT",
  "main": "build/index.js",
  "scripts": {
    "api:init": "cd api && ./init.sh",
    "api:dev": "cd api && ./run.sh",
    "api:prod": "cd api && ./run.sh",
    "lint": "eslint '*/**/*.{js,ts,tsx}' --quiet --fix",
    "build": "rm -rf build/* && tsc",
    "start": "ui-studio-builder"
  },
  "peerDependencies": {
    "react": "17.x",
    "react-dom": "17.x"
  },
  "uiStudio": {
    "openAPIEndpoint": "/api/v1/openapi.json",
    "componentPackages": [
      {{ #componentPackages }}
      "{{{ name }}}"{{ ^last }}, {{ /last }}      
      {{ /componentPackages }}
    ]
  }
}
