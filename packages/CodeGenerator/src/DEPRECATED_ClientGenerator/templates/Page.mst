{{=<% %>=}}
import React from 'react';
import ReactDOM from 'react-dom';
import styled from 'styled-components';

<% >recursive_import %>

const Page = styled.div`
  height: 100%;
  width: 100%;
`;

const <% type %>_<% name %> = () => {
  return React.createElement(Page, {}, [<% >recursive_element %>]);
};

export default <% type %>_<% name %>;