import React from 'react';
import styled from 'styled-components';

const Flex = styled.div`
  height: 100%;
  width: 100%;

  display: flex;

  {{{ css }}}
`;

const layout_{{{ name }}} = ({ children }) => React.createElement(Flex, { className: '{{{ classNames }}}' }, children);  

export default layout_{{{ name }}};