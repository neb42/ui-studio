# `@ui-studio/render-engine`

Library to generate a UI Studio app

## Usage

```
import { run } from '@ui-studio/render-engine';

generateCode(
  string, // Path to UI Studio app
  boolean, // Run in development mode
)
```
