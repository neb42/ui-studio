import { Component } from 'canvas-types';
import * as FunctionsPkg from 'functions-pkg';
{{ #components }}
import {{{ name }}} from '{{{ package }}}/build/Components';
{{ /components }}

export const Components: {
  [library: string]: { [component: string]: Component & { component: React.FC<any> }},
} = {
  'custom': FunctionsPkg,
  'functions-pkg': FunctionsPkg,
  {{ #components }}
  '{{{ package }}}': {{{ name }}},
  {{ /components }}
};