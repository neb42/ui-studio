{
  "name": "canvas-render-engine",
  "version": "0.1.1",
  "description": "> TODO: description",
  "author": "Ben McAlindin <bmcalindin@faculty.ai>",
  "homepage": "https://github.com/neb42/canvas#readme",
  "license": "MIT",
  "main": "build/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/neb42/canvas.git"
  },
  "scripts": {
    "lint": "eslint '*/**/*.{js,ts,tsx}' --quiet --fix",
    "copy-templates": "yarn copy-templates:server && yarn copy-templates:client && yarn copy-templates:client-code",
    "copy-templates:server": "cp -r src/ServerGenerator/templates build/ServerGenerator/templates",
    "copy-templates:client": "cp -r src/ClientGenerator/templates build/ClientGenerator/templates",
    "copy-templates:client-code": "cp -r src/ClientGenerator/code build/ClientGenerator/code",
    "build": "rm -rf build/* && tsc && yarn copy-templates"
  },
  "dependencies": {
    "fs-extra": "^9.1.0",
    "mustache": "^4.0.1"
  }
}
