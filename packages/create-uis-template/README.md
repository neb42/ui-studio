# `create-uis-template`

Command line tool to generate a new UI Studio template package.
Initialises a directory with files, packages, and git.

## Usage

Create a UI Studio template 

```
yarn create uis-template <directory>
```
